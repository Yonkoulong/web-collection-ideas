import styled, { css } from "styled-components";
import { Box, Typography, Button } from "@/shared/components";
import { primaryColor, whiteColor } from '@/shared/utils/colors.utils'

export const CreateCategoryFormWrapper = styled(Box)`
`

export const CreateCategoryForm = styled('form')`
    display: flex;
    flex-direction: column;
    gap: 16px;
`

export const CreateCategoryInputContainer = styled(Box)`
`

export const CreateCategoryFeatureWrapper = styled(Box)`
    display: flex;
    align-items: center;
    justify-content: space-between;
`