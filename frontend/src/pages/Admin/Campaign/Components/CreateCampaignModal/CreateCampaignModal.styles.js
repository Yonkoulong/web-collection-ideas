import styled, { css } from "styled-components";
import { Box, Typography, Button } from "@/shared/components";
import { primaryColor, whiteColor } from '@/shared/utils/colors.utils'

export const CreateCampaignFormWrapper = styled(Box)`
`

export const CreateCampaignForm = styled('form')`
    display: flex;
    flex-direction: column;
    gap: 16px;
`

export const CreateCampaignInputContainer = styled(Box)`
`

export const CreateCampaignFeatureWrapper = styled(Box)`
    display: flex;
    align-items: center;
    justify-content: space-between;
`